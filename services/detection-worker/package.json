{
  "name": "detection-worker",
  "version": "1.0.0",
  "description": "Fraud detection rule engine — scores transactions and detects fraud rings via Neo4j",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "jest --coverage --forceExit",
    "lint": "eslint src/ --ext .js"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "express": "^4.18.2",
    "ioredis": "^5.3.2",
    "neo4j-driver": "^5.17.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.2"
  }
}
